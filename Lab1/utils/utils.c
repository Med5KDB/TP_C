#include <stdio.h>

void read_i(int* x) {
    scanf("%d", x);
}
void print_i(int *x){
    printf("Notre entier est: %d", x);
}
// Float
void read_f(float* x) {
    scanf("%f", x);
}
void print_f(float *x){
    printf("Notre nombre flottant est: %f", x);
}

// Char
void read_c(char* x) {
    scanf("%f", x);
}
void print_c(char *x){
    printf("Notre caractère est: %c", x);
}